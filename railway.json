{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install -r requirements.txt"
    },
    "start": {
      "cmd": "uvicorn ai_search_api:app --host 0.0.0.0 --port $PORT"
    }
  }
  